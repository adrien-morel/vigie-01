# Dependencies of the GCP deployment only (VIGIE_STORAGE=firestore).
# Separate from requirements.txt: local development runs on the file backend and has no reason to
# install google-cloud's grpc stack. The import is lazy on the code side
# (backend/memory/persistence.py), so the absence of this package blocks nothing locally.
-r requirements.txt
# The project's only dependency that is not installed locally: the pin comes from the PyPI index
# (2026-08-23) and not from an environment where it has run. Consistent with the status of the
# Firestore backend, written but never executed against a real database — recheck on the first build.
google-cloud-firestore==2.28.1
